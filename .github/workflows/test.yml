name: Test GCD Script
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v3
      - name: テストスクリプトの実行
        run: |
          chmod +x gcd.sh
          chmod +x test.sh
          ./test.sh
